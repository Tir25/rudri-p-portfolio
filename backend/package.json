{
  "name": "backend",
  "version": "1.0.0",
  "description": "Backend API for Rudri's website",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "init-db": "node init-db.js",
    "setup-db": "node setup-db.js",
    "setup": "npm install && node setup-db.js && node init-db.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "test-db": "node test-db.js",
    "migrate": "node migrate.js",
    "migrate:up": "node migrate.js up",
    "migrate:status": "node migrate.js status",
    "migrate:create": "node migrate.js create",
    "cursor-connect": "node cursor-db-connect.js",
    "db-status": "node -e \"require('./models/db').checkConnection().then(connected => console.log(connected ? '✅ Database connected' : '❌ Database disconnected')).catch(err => console.error('❌ Error:', err.message))\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "bcrypt": "^5.1.1",
    "compression": "^1.8.1",
    "cookie-parser": "^1.4.7",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "express-rate-limit": "^7.1.5",
    "form-data": "^4.0.4",
    "helmet": "^7.1.0",
    "jsonwebtoken": "^9.0.2",
    "morgan": "^1.10.1",
    "multer": "^2.0.2",
    "pg": "^8.11.3"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  }
}
